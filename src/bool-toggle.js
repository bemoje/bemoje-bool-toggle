/**
 * Set a boolean to its opposite value and then returns it.
 * @param {boolean} bool - The boolean value
 * @returns {boolean}
 */
export default function boolToggle(bool) {
	return bool === true ? false : true
}
